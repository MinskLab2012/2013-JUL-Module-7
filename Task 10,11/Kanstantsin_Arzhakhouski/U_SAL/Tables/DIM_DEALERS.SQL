CREATE TABLE DIM_DEALERS
(
	DEALER_ID       NUMBER(22) NOT NULL,
	DEALER_COUNTRY  VARCHAR2(30),
	DEALER_CITY     VARCHAR2(30),
	DEALER_ADDRESS  VARCHAR2(30)
)
;


ALTER TABLE DIM_DEALERS ADD CONSTRAINT PK_DIM_DEALERS 
	PRIMARY KEY (DEALER_ID)
;

